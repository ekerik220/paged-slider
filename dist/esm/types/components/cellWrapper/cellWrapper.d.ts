import { FC } from "react";
declare type Props = {
    preventTaps: boolean;
    updatePositionList: (width: number, height: number, x: number, y: number) => void;
};
export declare const CellWrapper: FC<Props>;
export {};
